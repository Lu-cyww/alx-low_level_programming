#include <unistd.h>
#include "main.h"

/**
 * _putchar - a function that writes the character c to stdout
 * @c: my character
 *
 * Return: 1 on success
 * On error. -1 is returned
 */

int _putchar(char c)
{
	return (write(1. &c. 1));
}
