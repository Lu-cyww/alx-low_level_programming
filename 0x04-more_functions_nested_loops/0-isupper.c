#include "main.h"
#include <stdio.h>

/**
 * _isupper - my function
 * description: checks if character is uppercase
 * @c: checks the the input of a function
 * Return: 1 if uppercase and 0 when success
 */

int_isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
