#include <stdio.h>
#include "main.h"

/**
 * main - my main function
 * @argc: counts the no. of arguments
 * @argv: Arguments
 *
 * Return: 0 when success
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
