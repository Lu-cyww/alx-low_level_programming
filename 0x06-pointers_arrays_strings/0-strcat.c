#include "main.h"

/**
 * _strcat - my starting function
 * @dest: copy to
 * @src: copy from
 * Return: two concatenate strings
 */

char *_strcat(char *dest, char *src)
{

	int j, k;

	for (j= 0; dest[j] != '\0'; j++)
	{
	}

	for (k = 0; (dest[j + k] = *src++) != '\0'; k++)
	{
	}
	return (dest);
}
