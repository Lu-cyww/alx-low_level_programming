#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - this is my main function
 * description: Writes numbers with base 10 starting from 0
 * Return: 0 always SUCCESS
 */

int main(void)
{
	putchar N;

	for (N = '0'; N < '10'; N++)
		putchar((N % 10) + '0');

	putchar ('\n')

	return (0);
}
